{
  "start": {
    "label": "OSS Request Info",
    "type": "INPUT_NEEDED",
    "info": "Please fill in all the info for your OSS request.",
    "form": {
      "schema": {
        "title": "OSS Request Info",
        "type": "object",
        "properties": {
          "project_name": {
            "title": "Project Name",
            "type": "string",
            "required": true
          },
          "github_org": {
            "title": "GitHub Org",
            "type": "string",
            "enum": ["Foo", "Bar", "Personal"],
            "required": true
          }
        }
      },
      "options": {
        "fields": {
          "github_org": {
            "type": "select"
          }
        }
      }
    }
  },
  "ip_approval": {
    "label": "IP Approval",
    "type": "APPROVAL",
    "completable_by_email": "foo@bar.com",
    "info": "Schedule a meeting with Foo Bar to start the IP approval process."
  },
  "create_github_repo": {
    "label": "Create GitHub Repo",
    "type": "ACTION",
    "completable_by_email": "asdf@test.com",
    "info": "A GitHub repo needs to be created."
  }
}
